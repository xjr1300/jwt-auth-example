[package]
name = "middlewares"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.1"
actix-session = { version = "0.6", features = ["redis-rs-tls-session"] }
configurations = { path = "../configurations" }
tracing = "0.1"
